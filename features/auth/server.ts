/**
 * Solo Server Components / Route Handlers / middleware-adjacent.
 * No importar desde 'use client'.
 */
export {
  createSupabaseServerClient,
  getServerSessionUser,
} from '@/lib/auth/server-session';
