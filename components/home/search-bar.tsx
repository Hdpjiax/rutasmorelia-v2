'use client';

/**
 * Placeholder de extracción: la barra de búsqueda sigue en home-app
 * para evitar un refactor riesgoso de un solo golpe.
 * Importar desde aquí en siguientes splits.
 */
export type SearchField = 'origin' | 'destination';
