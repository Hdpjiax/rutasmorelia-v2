# Permitir scripts nativos que Next necesita (imágenes / resolución de módulos)
# pnpm 10+ bloquea builds por defecto si no están aprobados.
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*
