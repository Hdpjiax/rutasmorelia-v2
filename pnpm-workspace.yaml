# Workspace monorepo (este repo es la raíz de la app)
packages:
  - '.'
  - 'mobile'

# pnpm 10+/11: aprobar builds nativos que Next y tooling necesitan
allowBuilds:
  sharp: true
  unrs-resolver: true
  esbuild: true
